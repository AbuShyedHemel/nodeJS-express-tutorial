"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseFileContainer = void 0;
const constants_1 = require("../constants");
function parseFileContainer(filename) {
    const fileExtension = filename.split('.').pop();
    if (fileExtension) {
        return constants_1.FILE_CONTAINERS.find(({ container }) => container.toUpperCase() === fileExtension.toUpperCase());
    }
}
exports.parseFileContainer = parseFileContainer;

"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseCodecs = void 0;
const constants_1 = require("../constants");
function parseCodecs(filename) {
    const result = {};
    for (const codec of constants_1.CODECS_AUDIO) {
        for (const label of codec.labels) {
            if (filename.toUpperCase().includes(label.toUpperCase())) {
                result.audio = codec.codec;
            }
        }
    }
    for (const codec of constants_1.CODECS_VIDEO) {
        for (const label of codec.labels) {
            if (filename.toUpperCase().includes(label.toUpperCase())) {
                result.video = codec.codec;
            }
        }
    }
    return result;
}
exports.parseCodecs = parseCodecs;
